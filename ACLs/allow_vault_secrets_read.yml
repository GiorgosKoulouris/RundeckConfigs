by:
  group: runJobUsers
description: Allows runJobUsers the ability to read Project Key Storage entries for vault authentication.
for:
  storage:
  - allow:
    - read
    match:
      path: keys/vault(/.*)?
context:
  application: rundeck